#include <math.h>

double find_sum_elements_series(int k){
    /*if (k < 0){
        return -1;
    }
    if (k == 0){
        return 0;
    }
    else{
        return (double)find_sum_elements_series(k-1) + (double)(find_sum_elements_series(k - 1) + 1.0)/(double)(k);
    }*/
    return 1.0;
}
