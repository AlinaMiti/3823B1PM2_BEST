#include <math.h>

void find_loops(int* pic, int size, int* num_loop, int* start_row, int* start_col, int* finish_row, int* finish_col){
    
    /*
    int razm = sqrt(size);
    int stolbik = 0;
    for (int i = 0; i < size; i+=razm){{
            if (pic[i] == 1){
                *start_row = i/razm;   
                *start_col = 0;   
        }        
    }
    }
    int petli  = 0;
    for (int f = 0; f < size-start_col; f++){
        if (pic[start_row][f] == 1 && pic[start_row-1][f]==1){
            petli++;
        }
    }
    *num_loop = petli;

        for(int i = size; i > 0; i--){
                if (pic[start_row][i] == 0 && pic[start_row][i-1] == 1){

                }
            
            }*/
             return;
        }
        
   

