#include <math.h>

void find_loops(int* pic, int size, int* num_loop, int* start_row, int* start_col, int* finish_row, int* finish_col){
    /*int start(int pic, int size){
    for (int i = 0; i < size; i++){
        for (int j = 0; j < size; j++){
            if (pic[j][i] == 1){
                start_row = j;   
                start_col = i;
                return (start_row, start_col);
                //continue;
        }
        }
    }
    }
    int loop(int pic, int size, int start_row, int start_col){
    for (int f = 0; f < size-start_col; f++){
        if (pic[start_row][f] == 1 && pic[start_row-1][f]==1){
            num_loop++;
        }
    }
    return num_loop;
    }
    int finisch(int pic, int start_row){
        for(int i = 0; i < size-start_col; i++){
            for(int t = 1; t<size-i; t++){
                if (pic[start_row][i] == 1 && pic[start_row][i+t] == 0)
                    return (start_row, i);
            }
        }
    }
*/
    return 0;
}
